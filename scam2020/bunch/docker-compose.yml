version: "3.2"
services:
    base:
        build: 
            context: .
            dockerfile: dockerfile/Dockerfile.bunch.base
        container_name: scam2020_bunch

    experiment:
        build: 
            context: .
            dockerfile: dockerfile/Dockerfile.bunch
        volumes:
            - ../graphs:/go/src/data/graphs
            - ../experiments/bunch/report:/go/src/exp
            - ../experiments/bunch/log:/go/src/log
        container_name: scam2020_bunch_experiment
        depends_on:
            - base
        deploy:
            resources:
                limits:
                    cpus: '3'
                    memory: 3gb

